name: Build and Deploy Code

# Define the events that trigger this workflow
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

# Define the jobs to be executed
jobs:
  job1:
    # Specify the runner environment for the job
    runs-on: ubuntu-latest
    
    # Define the sequence of steps for job1
    steps:
      # Step 1: Checkout the repository code
      - name: Checkout repository
        # Use the standard action to fetch the code
        uses: actions/checkout@v4
        
      # Step 2: A simple command to verify the job is running
      - name: Say hello
        run: echo "Hello world, the pipeline is running!"
      
      # NOTE: This is where you would typically add steps for building 
      # your code (e.g., setting up Node.js, running npm install/build) 
      # and then deploying it.